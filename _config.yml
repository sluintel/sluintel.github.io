# GitHub Pages configuration
permalink: pretty

# Tell Jekyll to copy these files to _site untouched — critical for sitemap.xml
keep_files:
  - sitemap.xml
  - robots.txt

# Exclude files that shouldn't be published
exclude:
  - scripts/
  - requirements.txt
  - used_keywords.json
  - README.md
